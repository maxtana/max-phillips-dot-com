---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-GB', {
			year: '2-digit',
			month: 'short',
			day: '2-digit',
		})
	}
</time>